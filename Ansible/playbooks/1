---
  - name: "Create DPG Application Playbook"
    hosts: localhost
    connection: local

    vars_files:
      - variables.yaml

    tasks:

    - name: "User Creation"
      thales.ciphertrust.usermgmt_user_create:
        localNode: "{{ this_node_connection_string }}"
        username: "pagal"
        password: "Entrust@2018"
      register: output

    - name: Debug Output
      debug: var=charset
